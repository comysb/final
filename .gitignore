# ── 음성 데이터 (환자 개인정보) ──────────────────────
*.wav
augmented_wavs/

# ── 임베딩 CSV (환자 데이터 포함) ───────────────────
features_emb_all.csv
features_emb_aug_pool.csv

# ── 대용량 모델 가중치 (.pt) ─────────────────────────
models/master/*.pt

# ── PDF (저작권) ─────────────────────────────────────
*.pdf

# ── 엑셀 데이터 ──────────────────────────────────────
*.xlsx

# ── Python 캐시 ──────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── 로컬 설정 ────────────────────────────────────────
.agents/
.antigravityignore
